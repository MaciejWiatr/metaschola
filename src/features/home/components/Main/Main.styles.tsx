import tw from "twin.macro";

export const HomeMain = tw.main`
	flex flex-col flex-1 py-8 w-full items-stretch max-w-2xl`;

export const LoadingStatus = tw.div`
	flex items-center justify-center w-full h-24
	text-center
	`;
